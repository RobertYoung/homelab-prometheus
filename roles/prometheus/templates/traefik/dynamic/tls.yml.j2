tls:
  certificates:
    - certFile: /certs/{{ prometheus_service_domain }}.crt
      keyFile: /certs/{{ prometheus_service_domain }}.pem
  options:
    default:
      minVersion: VersionTLS12
      sniStrict: true
