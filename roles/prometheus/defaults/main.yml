---
# Prometheus configuration
prometheus_user: prometheus
prometheus_config_dir: /etc/prometheus
prometheus_data_dir: /var/lib/prometheus
prometheus_service_name: prometheus
prometheus_port: 9090
prometheus_version: latest

# Traefik / service exposure
prometheus_service_domain: prometheus.local.iamrobertyoung.co.uk
prometheus_service_port: "443"
prometheus_traefik_config_dir: /etc/traefik

# Retention and storage
prometheus_retention_time: "15d"
prometheus_retention_size: "0"  # 0 means no size limit

# Scrape configuration
prometheus_scrape_interval: "15s"
prometheus_evaluation_interval: "15s"
